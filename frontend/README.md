# Фронтенд юридического ассистента

Веб-интерфейс для юридического ассистента на основе LLM.

## Технологии

- **React 18** - библиотека для построения UI
- **TypeScript** - статическая типизация
- **Vite** - быстрый сборщик
- **Tailwind CSS** - утилитарный CSS фреймворк
- **shadcn/ui** - библиотека компонентов
- **Framer Motion** - анимации
- **React Router** - роутинг

## Быстрый старт

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр production сборки
npm run preview
```

## Переменные окружения

Создайте файл `.env`:

```env
VITE_API_URL=http://localhost:8000
```

## Структура

```
src/
├── components/          # React компоненты
│   ├── chat/           # Компоненты чата
│   ├── layout/         # Компоненты макета
│   └── ui/             # UI компоненты (shadcn/ui)
├── hooks/              # Кастомные React хуки
├── lib/                # Утилиты и API клиент
├── pages/              # Страницы приложения
├── App.tsx             # Главный компонент
└── main.tsx            # Точка входа
```

## Основные функции

- 💬 Чат-интерфейс для общения с ассистентом
- 📚 Отображение источников (статей законов)
- 🎨 Современный и отзывчивый дизайн
- ⚡ Быстрая загрузка и плавные анимации
- 📱 Адаптивный дизайн для мобильных устройств

## API интеграция

Фронтенд взаимодействует с бэкендом через REST API:

- `POST /generate` - генерация ответа на вопрос с источниками

Клиент API находится в `src/lib/api.ts`.
